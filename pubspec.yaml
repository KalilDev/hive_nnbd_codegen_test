name: hive_nnbd_codegen_test
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  hive: ^2.0.0-nullsafety.0

dev_dependencies:
  build_runner:
  hive_generator: ^1.0.0-nullsafety.0
  pedantic: ^1.9.0
  test: ^1.14.4

dependency_overrides:
  hive:
    path: ../hive
  hive_generator:
    path: ../hive_generator